@AGENTS.md

# 模型分工

- **Fable 5.1（主会话）负责**：方案设计与技术选型、问题排查与根因分析、前端交互与视觉审美设计。产出是明确的方案、拆解后的任务说明和审查结论。
- **Opus 负责具体编码**：通过 Agent 工具派生子代理并指定 `model: "opus"`，把完整需求、涉及文件、验收标准交给它落地写代码。
- **小改动例外**：一两行修复、改文案这类很小的改动，Fable 直接做，不必绕给 Opus。
- **验收强制**：Opus 写完后，Fable 必须审查 diff 并跑类型检查和构建，确认通过后再向用户汇报；不通过则打回重做。
